export default {
    SET_NEWS(state, news) {
        state.news = news;
    },
    SET_JOBS(state, jobs) {
        state.jobs = jobs;
    },
    SET_ASKS(state, asks) {
        state.asks = asks
    },
    SET_USER(state, users) {
        state.user = users
    },
    SET_ITEM(state, item) {
        state.item = item
    },
    SET_LIST(state, list) {
        state.list = list;
    }
    
}